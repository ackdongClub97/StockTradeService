# 버전 업데이트 기록 (2026-07-30)

블로그에 진행 상황을 정리하기 위한 상세 메모. 이번 버전업에서 요청한 기능/환경 변경과, 그 과정에서 실제로 겪은 이슈를 문제/원인/해결 형식으로 같이 정리했다.

---

## 1. 신규 기능

### 1-1. 매수 지정가(지정가 매수) 도입

**배경/요구사항**
기존엔 매도 주문에만 "현재가 / 지정가" 토글이 있었고, 매수는 무조건 현재가 기준으로만 주문 가능했음. 여기에 매수 지정가를 추가하되, 단순히 "지정한 가격 이하면 체결"에서 그치지 않고 "거시경제학적으로" 호가 데이터 기반의 자동 가격개선 로직을 요청받음: 지정한 금액보다 더 낮은 금액에 물량이 많으면 자동으로 그 낮은 가격에 체결되게 하되, 무제한으로 좋아지는 건 이상하니 제한(5% 이하)을 두고 싶다는 요구.

**설계 결정 (대화로 확정된 것)**
- 5% 제한은 **주문 자체를 거부하는 조건이 아니라, 자동 가격개선이 일어날 수 있는 범위 제한**이다. 즉 지정가의 95%~100% 구간 안에 있는 매도호가만 자동으로 잡아먹고, 그보다 싼 호가(95% 미만)는 무시하고 미체결로 남긴다.
- 이번 라운드는 **매수만** 적용, 매도는 기존 로직 그대로 유지.
- 이 5% 밴드는 **지정가 모드 전용**이다. 현재가(시장가) 매수는 밴드 없이 "지정가(=주문 시점 현재가) 이하 호가면 그대로 체결"하는 단순 로직으로 남겨둔다. (사용자가 "이건 일반 매수에도 적용되는 게 아니라 다른 기능으로 존재해야 해"라고 명시적으로 요청한 부분)

**구현**
- `Order`에 `priceMode`(`PriceMode` enum: `MARKET` / `LIMIT`) 필드 추가 — 이전엔 백엔드가 현재가/지정가 여부를 아예 구분할 방법이 없었음(프론트에서만 상태로 갖고 있었음).
- `MatchingService.isEligible(levelPrice, order, isBuy)`: 매수(`isBuy=true`)이고 `priceMode == LIMIT`일 때만 `levelPrice >= ceil(order.price * 0.95)` 하한 조건을 추가로 검사. `MARKET`이거나 매도인 경우는 기존과 동일하게 `levelPrice <= order.price`(매수) / `levelPrice >= order.price`(매도)만 검사.
- 프론트(`stockDetail.html`): 매수 모달에 매도와 동일한 패턴(현재가/지정가 탭, 단가 입력, 최종 금액 표시)을 새로 구성(`buyPriceSection`), 기존의 "자동 계산된 총액만 보여주던" `orderPrice` 필드는 제거하고 `buyUnitPrice`/`buyTotalAmount`로 교체.
- 매수 지정가 탭에 안내 UI 추가: "자동 가격개선 (최대 5% 낮은 가격까지)" 체크박스(항상 체크된 표시용) + 동작 설명 문단 + "지정하신 가격 또는 그보다 낮은 가격에 반드시 체결되는 것은 아닙니다" 경고 문구(⚠️, 주황색).

**검증**
- `MatchingServiceTest`에 시나리오 기반 유닛 테스트 4건:
  1. 부분체결 성공(여러 회차에 걸쳐 결국 전량 체결)
  2. 부분체결 일부 실패(호가 잔량 부족으로 일부만 체결)
  3. 지정가 매수 — 밴드(95~100%) 안의 호가만 자동 체결, 밴드 밖은 미체결로 남음
  4. 현재가 매수 — 같은 호가 상황에서 밴드 제한 없이 전부 체결
- `./gradlew test`로 4건 전부 통과 확인.
- 실제 브라우저로 매수 모달 클릭 테스트는 안 함(코드/테스트 레벨 검증까지만 완료).

---

### 1-2. 작은 화면에서 주문 팝업이 잘리는 문제 수정

`issue.md`에 문제/원인/해결로 기록된 내용과 동일. 요약:
- 지정가 UI(탭/체크박스/설명/경고 문구)가 추가되면서 `#tradeModal .modal-content`의 세로 길이가 늘어났는데, `.modal-content`에 `max-height`/`overflow` 속성이 없어서 뷰포트가 작은 화면에서는 팝업 하단(주문 버튼 포함)이 화면 밖으로 밀려 잘리고 스크롤도 안 되는 상태였음.
- `.modal-content`에 `max-height` + `overflow-y: auto`(+`overflow-x: hidden`) 추가. 처음엔 `85vh`로 잡았는데, "화면이 큰 환경에서도 팝업 기본 높이가 줄어드는 것처럼 보인다"는 피드백을 받아 `94vh`(모바일 `92vh`)로 재조정해서, 화면이 실제로 부족할 때만 스크롤이 개입하도록 함.
- 실제 작은 뷰포트에서의 렌더링 재현 테스트는 아직 브라우저로 안 해봄(CSS 변경이라 컴파일은 불필요, 다음에 실기기/뷰포트 축소로 확인 필요).

---

### 1-3. 미체결 주문 모니터링 + 어드민 화면

**배경/요구사항**
"백오피스/운영 에이전트"를 만들고 싶다는 요청에서 시작. 우선순위를 미체결 주문부터로 정하고, 다음 세 가지 원인을 구분해서 보여달라는 구체적인 요구를 받음:
1. 호가가 매수/매도 지정 금액에 안 맞아서 체결이 안 된 것 (정상적인 시장 상황)
2. 그 시점에 데이터 전송이 안 돼서(연결 문제) 비교할 가격 자체가 없어서 체결이 안 된 것
3. 그 시점 접속자/주문이 많아서(시스템 부하) 체결이 안 된 것

추가로 어드민 화면에서 지정 시점의 로그를 볼 수 있고, 오류 발생 시 알려주는 기능(모니터링)을 요청받음.

**구현**
- `UnmatchReason` enum(`NO_ELIGIBLE_PRICE` / `NO_ORDERBOOK_DATA` / `SYSTEM_ERROR`) + `Order.lastUnmatchedReason`, `Order.lastAttemptAt` 필드 추가. 매칭 시도가 있을 때마다(`matching()` 30초 배치, `tryMatch()` 즉시 시도, `execute()` 실제 체결 시도) 이 필드를 갱신해서, 로그를 뒤지지 않아도 주문 테이블만 봐도 "왜 안 풀렸는지"가 남게 함.
  - `NO_ELIGIBLE_PRICE`: `computeFill()`이 null 반환(호가 조건 불충족) → 3번 케이스는 아니고 위 1번 케이스에 해당.
  - `NO_ORDERBOOK_DATA`: `kisWebSocketService.getCachedAskingPrice()`가 null(웹소켓 틱이 아직 없음/연결 문제) → 2번 케이스.
  - `SYSTEM_ERROR`: 매칭 시도 중 예외(주로 DB/HikariCP 관련) 발생 → 3번 케이스(접속자·주문 폭주 시 전형적으로 나타나는 실패 양상)에 대응.
- `MatchingService.matching()`의 종목별 처리와 `execute()`를 각각 try/catch로 감싸서, 하나가 예외를 던져도 그 배치의 나머지 종목/주문은 계속 처리되도록 격리(기존엔 예외 하나가 30초 배치 전체를 조용히 끊을 수 있는 잠재적 결함이었음).
- 새 `admin` 패키지: `AdminController`(`/admin/orders` 페이지, `/api/admin/logs` 로그 조회 API, `/api/admin/alerts/stream` SSE), `AdminAlertService`(SSE 알림 싱크, 기존 주문결과 스트림과 동일한 `Sinks.Many` 패턴), `LokiClient`(Loki HTTP API `query_range`를 직접 호출하는 WebClient 래퍼 — Grafana 없이도 관리자 화면에서 바로 로그 조회 가능).
- `adminOrders.html`/`adminOrders.css`: 미체결 주문 테이블(주문ID/회원/종목/구분/수량/체결/가격/상태/미체결사유/마지막 시도), 행마다 "로그 보기" 버튼(해당 주문의 `lastAttemptAt` 전후 10분 로그를 Loki에서 조회해 모달로 표시), `SYSTEM_ERROR` 발생 시 페이지가 열려있으면 실시간 배너로 알림.
- 접근 제어: 이 프로젝트엔 역할(Role) 체계가 없어서(`CustomerDetails.getAuthorities()`가 항상 `ROLE_USER`만 반환) 새로 권한 시스템을 만드는 대신, `application.yaml`의 `admin.member-id`(env `ADMIN_MEMBER_ID`)와 로그인한 회원의 memberId를 비교하는 방식으로 최소한으로 구현. 기본값이 비어있어서 설정 안 하면 아무도 못 들어감.

**검증**
- `./gradlew compileJava` / `compileTestJava` 통과.
- 기존 `MatchingServiceTest`에 새로 추가된 `AdminAlertService` 의존성에 대한 `@Mock`을 빠뜨렸던 걸 발견해서 추가(자세한 내용은 아래 "발생한 이슈" 참고).
- 실제로 서버를 띄워서 `/admin/orders` 화면을 브라우저로 열어보거나, 일부러 예외를 발생시켜 SSE 알림 배너가 뜨는지까지는 확인 안 함 — 코드/컴파일 레벨 검증까지만 완료.

---

## 2. 환경/인프라 변경

### 2-1. Grafana + Loki 로그 수집 구성 (로컬)

- `observability/docker-compose.yml`: Loki + Grafana 2개 컨테이너만 구성(Promtail 없음 — 앱이 로그를 Loki에 직접 push하는 구조라 파일 tail이 필요 없음).
- 앱 쪽: `build.gradle`에 `com.github.loki4j:loki-logback-appender:1.5.2` 추가, `logback-spring.xml` 신규 작성 — 콘솔 로그는 그대로 두고 Loki appender를 추가로 등록(라벨: `app=stocktrade,host=...,level=...`). Loki가 안 떠있어도 비동기 전송이라 앱 구동/요청 처리엔 영향 없음.
- Grafana에 Loki 데이터소스를 수동 설정 없이 자동으로 잡히도록 프로비저닝 파일(`observability/grafana/provisioning/datasources/loki.yml`) 추가, 로컬 테스트 편의를 위해 익명 로그인 허용.
- **로그/대시보드 영속화**: 처음 구성 땐 볼륨을 안 붙여서 컨테이너를 지웠다 새로 만들면 로그가 그대로 사라지는 상태였음. "로그 남겨서 나중에 모니터링 만들 것"이라는 요청을 받고 `loki-data`(Loki `/loki` 전체 경로), `grafana-data`(Grafana `/var/lib/grafana`, 나중에 만들 대시보드도 유지) 두 개의 named volume 추가.
- 로그 일부를 파일로 뽑아내고 싶다는 요청에 맞춰 `observability/export-logs.sh` 작성 — Loki의 `query_range` API를 curl로 호출하고 `jq`로 파싱해서 `타임스탬프\t로그내용` 형식의 텍스트 파일로 저장. 쿼리/기간/출력파일명을 인자로 받음.

### 2-2. 논의했지만 지금은 보류한 것

- **DB를 MySQL로 전환**: 실사용자를 받을 계획이 생기며 검토. 다만 지금 겪고 있는 HikariCP 커넥션 풀 이슈는 DB 엔진의 한계가 아니라 앱이 짧은 시간에 DB를 과하게 두드리는 패턴 때문이라, MySQL로 바꿔도 풀 크기가 그대로면 똑같이 고갈될 수 있다고 판단 → 이번엔 보류. (대신 MySQL 전환 시 얻는 것/드는 비용은 논의만 하고 기록해둠: 진짜 클라이언트-서버 DB라 다중 인스턴스 동시접속 가능, 백업/복제 생태계 성숙 vs 별도 서버 운영 부담·마이그레이션 비용)
- **HikariCP `maximum-pool-size` 10 → 20 증설**: 근본 원인(추정: 좀비 SSE 스트림 — `issue.md` 참고)을 안 고친 채 숫자만 올리면 고갈까지 걸리는 시간만 늘어날 뿐 재발 자체는 못 막는다고 판단해서 보류. HikariCP 공식 가이드도 무조건 크게 잡는 걸 권장하지 않음(과도하게 크면 컨텍스트 스위칭/경합으로 오히려 손해일 수 있음).
- 대신 **관찰을 먼저 하기로 결정**: 이번에 만든 어드민 화면(`SYSTEM_ERROR` 사유 빈도) + Grafana/Loki로 실사용자 트래픽 하에서 실제로 재발하는지 지켜본 뒤, 확인되면 근본 수정(SSE 구독 정리 로직 점검 등) 진행. `issue.md`의 "진행 상황 (2026-07-30)" 섹션에 기록.

---

## 3. 이번 버전업 과정에서 발생한 이슈

### 3-1. (issue.md 기록) 작은 화면에서 주문 팝업이 잘림
- **문제**: 매수 지정가 UI 추가로 팝업이 길어지면서, 뷰포트가 작은 환경에서 팝업 하단이 화면 밖으로 밀려 잘리고 스크롤도 안 됨.
- **원인**: `.modal-content`에 `max-height`/`overflow` 속성이 아예 없었음. 배경 오버레이(`.modal`)도 스크롤 설정이 없어서 초과분이 그냥 시각적으로 사라지는 구조였음.
- **해결**: `.modal-content`에 `max-height` + `overflow-y: auto` 추가. 첫 시도(`85vh`)가 평소 화면에서도 기본 높이를 줄인 것처럼 보인다는 피드백을 받아 `94vh`(모바일 `92vh`)로 재조정.

### 3-2. (issue.md 기록, 이번에 재점검) DB 커넥션 풀(HikariCP) 고갈
- 이전 세션에서 발견된 이슈(마이페이지 매매차익 조회 500 에러, 풀 10개 전부 active로 고갈)를, 실사용자 계획을 계기로 다시 점검함.
- 이번 세션에서 새로 확인된 것: **정확한 누수 원인은 여전히 특정 못했고, 재발 인시던트가 없었던 게 "괜찮다"는 뜻은 아니라는 점**을 명시적으로 기록. MySQL 전환/풀 사이즈 증설 모두 근본 해결책이 아니라고 판단해 보류하고, 관찰(어드민 화면 + Loki)을 우선하기로 결정.

### 3-3. (이번 세션에서 직접 겪은 것, issue.md엔 없음) Grafana/Loki 볼륨 누락
- **문제**: `observability/docker-compose.yml`을 처음 구성했을 때 Loki/Grafana에 볼륨을 안 붙여서, 컨테이너를 삭제 후 재생성하면 지금까지 쌓인 로그와 Grafana 설정이 전부 날아가는 상태였음.
- **원인**: 데이터 영속화를 처음부터 고려하지 않고 최소 구성으로만 만듦.
- **해결**: `loki-data`(Loki `/loki`), `grafana-data`(Grafana `/var/lib/grafana`) named volume 추가. `docker compose config`로 문법 검증 완료.

### 3-4. (이번 세션에서 직접 겪은 것, issue.md엔 없음) MatchingServiceTest에 신규 의존성 Mock 누락
- **문제**: `MatchingService`에 `AdminAlertService` 의존성을 새로 추가했는데, 기존 `MatchingServiceTest`엔 이 타입에 대한 `@Mock`이 없었음.
- **원인**: Mockito의 생성자 주입(`@InjectMocks`)이 매칭되는 mock이 없는 파라미터엔 그냥 `null`을 넣어주기 때문에, 테스트 자체는 예외 없이 통과해버림(정상 체결 경로에서는 `adminAlertService`가 호출되지 않아서 우연히 안 드러난 것). 즉 지금 당장은 안 터지지만, 나중에 예외 경로를 테스트하는 케이스가 추가되면 `NullPointerException`으로 터질 잠재적 결함이었음.
- **해결**: `@Mock private AdminAlertService adminAlertService;`를 테스트 클래스에 명시적으로 추가. 재실행해서 4건 전부 통과 확인.

### 3-5. (사소한 구현 중 이슈) MatchingService 컴파일 오류
- 5% 밴드 로직을 `PriceMode`로 분기하도록 고치면서 `MatchingService.java`에 `import stockOrder.stockTrade.order.PriceMode;`를 빠뜨려 컴파일 에러 발생 → import 추가 후 즉시 해결. 기록할 만큼 큰 이슈는 아니지만 구현 로그 차원에서 남겨둠.

---

## 4. 아직 검증 안 된 것 (다음에 확인 필요)

- Grafana/Loki 로그 파이프라인을 실제로 앱을 띄운 상태에서 end-to-end로 확인 안 함(의존성 해석/컴포즈 문법 검증까지만 함). `docker compose up -d` 후 `bootRun`으로 실제 로그가 Loki에 쌓이는지 확인 필요.
- 어드민 화면(`/admin/orders`)을 브라우저로 직접 열어보거나, 일부러 예외를 발생시켜 실시간 알림 배너가 뜨는지 확인 안 함.
- 작은 화면에서의 팝업 스크롤 수정을 실제 축소된 뷰포트로 재현 테스트 안 함.

---

## 5. 커밋/배포 상태

- 매수 지정가 기능 + 관련 테스트 + README/issue.md 갱신 → 커밋 및 `origin/main` 푸시 완료 (커밋 `b4c2942`)
- 미체결 주문 모니터링(어드민 화면) + Grafana/Loki 구성 + issue.md 업데이트 → 아직 커밋 전

---

## 6. FDS(이상거래탐지) 미니 버전 (2026-07-31)

### 배경/요구사항

취업 준비 목적으로 이 프로젝트에 "이상거래탐지(FDS)" 경험을 쌓아보고 싶다는 요청에서 시작. 실제 금융권 FDS도 대부분 규칙 기반으로 먼저 시작해서 설명 가능성과 빠른 배포를 확보한 뒤, 이후 통계/ML 기반으로 고도화하는 경우가 많다는 점을 근거로 이번에도 규칙 기반부터 시작하기로 결정.

### 운영 루프 구조를 먼저 고정한 이유

기능을 바로 짜기 전에 "이상 신호 감지 → 지표·로그로 원인 파악 → 가설 검증 → 반영·롤백" 4단계 루프를 먼저 구조로 확정하고, 그 다음에 각 단계를 무엇으로 채울지 결정하는 순서로 진행했다. 이 순서를 택한 이유:

- 실제 프로덕션 ML/AI 서비스 운영도 "감지 → 원인분석 → 검증 → 반영/롤백"의 반복 루프로 이루어지는 게 표준적인 패턴이고, 이 루프를 먼저 세워두면 "왜 이 기술을 골랐는지"를 단계별로 독립적으로 설명할 수 있게 된다. 기능부터 짜고 나중에 구조를 끼워맞추면 각 결정의 근거가 흐려지는데, 반대 순서로 하면 각 단계의 결정이 서로 독립적으로 검증 가능해진다.
- 구조를 먼저 확정하면서 원래 참고했던 "TTFT·KV Cache" 같은 지표를 그대로 못 쓴다는 것도 이 시점에 드러났다(아래 2단계 참고) — 구조부터 잡았기 때문에 "이 프로젝트에 실제로 존재하지 않는 지표를 억지로 끼워맞추는" 실수를 미리 걸러낼 수 있었다.

### 단계별 기술 선택 이유

**1단계 — 이상 신호 감지: 규칙 기반 + 기존 `AdminAlertService`(→ Grafana) 재사용**
- *왜 규칙 기반인가*: 통계/ML 기반 이상탐지는 학습 데이터가 필요한데, 이 프로젝트엔 실사용자 트래픽이 거의 없어 학습 데이터 자체가 없다. 규칙 기반은 "왜 이 알림이 떴는지"를 코드만 보고 100% 설명할 수 있다 — 규제/신뢰가 중요한 금융 도메인에서는 정확도보다 설명 가능성이 먼저라고 판단했다.
- *왜 새 알림 채널을 안 만들었는가*: `SYSTEM_ERROR` 알림에 이미 검증된 SSE 파이프라인(`AdminAlertService` → `/admin/orders` 실시간 배너)과 Grafana/Loki 연동이 있는 상태에서 새 채널을 또 만들면, 운영자가 여러 화면을 오가야 하는 문제가 생긴다. 하나의 알림 채널로 통합하는 쪽이 운영 관점에서 낫다고 판단해 그대로 재사용했다.

**2단계 — 지표·로그로 원인 파악: TTFT·KV Cache 대신 규칙별 실제 신호값 + 기존 Loki**
- 처음 참고했던 구조도에는 "TTFT·KV Cache"가 예시로 있었지만, 이 프로젝트는 자체 호스팅 LLM 서빙 스택(vLLM/Triton 등)이 없어서 그 지표 자체가 존재하지 않는다. 없는 지표를 있는 것처럼 문서에 남기면 인터뷰에서 검증 불가능한 주장이 되므로, 실제로 이 프로젝트에 존재하는 신호로 바꿨다: 각 규칙이 트리거될 때 로그에 남기는 실측값(주문 빈도 수, 왕복매매 간격, 가격 이탈율).
- 별도 메트릭 시스템(Prometheus 등)을 새로 붙이지 않은 이유: 이미 있는 Loki 로그 기반 조회로 충분히 원인 추적이 가능한 규모에서, 새 지표 시스템을 붙이는 비용이 얻는 이득보다 크다고 판단했다(트레이드오프 판단).

**3단계 — 가설 검증: Mockito 기반 시나리오 재현 테스트**
- 이 프로젝트엔 아직 실제 이상거래 사례가 없어서, 임계값을 넘는/안 넘는 상황을 코드로 직접 구성해 재현하는 방식을 택했다. 새 테스트 방법론을 도입하지 않고 기존 `MatchingServiceTest`와 동일한 패턴(Mockito + `@InjectMocks`)을 그대로 따랐다.
- 규칙마다 "정상 케이스(알림 안 뜸)"와 "이상 케이스(알림 뜸)"를 반드시 짝으로 작성했다 — 한쪽만 검증하면 임계값이 타당한지(오탐/누락 여부) 판단할 수 없기 때문.

**4단계 — 반영·롤백: 차단 없음 + `fds.enabled` 설정 스위치**
- *왜 차단이 아니라 로그+알림만 하는가*: 검증되지 않은 임계값으로 정상 사용자의 주문을 막으면 서비스 신뢰도에 직접 타격을 준다. 1차 버전에서는 "관찰만 하고 절대 차단하지 않는다"를 원칙으로 세웠다.
- *왜 코드 롤백이 아니라 설정값 스위치를 뒀는가*: 오탐이 몰릴 경우 코드를 되돌리고 재배포하는 빌드-배포 사이클 동안 계속 오탐 알림이 쌓이는 것보다, `application.yaml`의 `fds.enabled` 값(또는 배포 환경변수) 하나만 바꿔서 즉시 끌 수 있는 게 훨씬 안전하다. "배포 구조를 확정한다"는 게 코드 자체가 아니라 이 스위치를 어디에 둘지를 결정하는 것이었다.

### 구현 내용

**탐지 규칙 3가지**
1. **주문 빈도 이상** — 회원별 최근 주문 시각을 슬라이딩 윈도우로 추적(`KisService`의 `ConcurrentHashMap` 캐시 스타일 그대로 차용), 60초 내 10건 이상이면 알림.
2. **초단타 왕복매매** — 체결 시점에 같은 회원·종목에서 반대 방향(`OrderType`) 체결이 60초 내에 있었는지 `TradeRepository`로 조회, 있으면 알림.
3. **가격 이상치** — 지정가(`PriceMode.LIMIT`) 주문가가 `KisService.getCachedStockData()`로 조회한 현재가 대비 20% 이상 벗어나면 알림. 시장가 주문은 대상 아님.

**적용 방식**
- 새 패키지 `fds` — `admin` 패키지와 같은 구조로 `FraudDetectionService` 하나만 신설(룰이 3개뿐이라 별도 인터페이스 없이 메서드로 분리).
- 훅 포인트: 주문 생성 시점(`OrderController.submitOrder`)에서 1·3번 규칙, 체결 시점(`MatchingService.execute()`)에서 2번 규칙.
- `AdminAlert`(기존 레코드) 그대로 재사용해서 `AdminAlertService`로 발행 — 새 알림 타입/화면 추가 없음.
- `fds.enabled`(기본 `true`) + 규칙별 임계값을 `application.yaml`에 설정값으로 분리(`fds.order-frequency.*`, `fds.round-trip.*`, `fds.price-outlier.*`).
- FDS 자체 오류가 주문 흐름을 막지 않도록 `checkOnSubmit`/`checkOnExecute`를 각각 try-catch로 감싸서, 탐지 로직이 실패해도 주문 생성/체결은 정상 진행되게 격리.

> **임계값에 대한 참고**: 60초/10건, 60초, 20%는 실측 데이터가 아니라 초기 추정값이다. 실사용자 트래픽이 쌓이면 Grafana로 실제 분포를 보고 재조정할 계획.

### 재현 테스트 (가설 검증)

`FraudDetectionServiceTest` 8건 — 규칙별 정상/이상 케이스 쌍 + `fds.enabled=false` 롤백 스위치 동작까지 확인:
- 빈도: 9건 무알림 → 10번째 주문에서 알림
- 가격 이상치: 42.9% 이탈 시 알림 / 1.4% 이탈 시 무알림 / 시장가는 규칙 자체를 건너뜀
- 왕복매매: 반대 방향 체결 이력 있음 → 알림 / 없음 → 무알림
- `fds.enabled=false` → 임계값을 훨씬 넘는 입력을 줘도 전부 무알림 (즉시 롤백 가능함을 테스트로 보증)

기존 `MatchingServiceTest`에 새로 추가된 `FraudDetectionService` 의존성 `@Mock`도 함께 추가(3-4에서 기록한 것과 같은 종류의 실수를 이번엔 처음부터 피함).

### 검증

- `./gradlew compileJava compileTestJava` 통과.
- `./gradlew test`로 `FraudDetectionServiceTest` 8건, `MatchingServiceTest` 4건 전부 통과 확인.
- 실제 서버를 띄워 브라우저로 어드민 알림 배너에 FDS 알림이 뜨는지까지는 아직 확인 안 함 — 코드/테스트 레벨 검증까지만 완료.

### 상태

- 설계 + 구현 + 재현 테스트까지 완료. `upgrade.md`의 백로그 상태도 갱신 필요.


*** 기능 추가 ***

## 1. FDS(이상거래탐지) 미니 버전 — 완료

주문 패턴에서 이상 거래(빈도 이상, 초단타 왕복매매, 가격 이상치 등)를 탐지하는 규칙 기반 필터. JD의 "FDS(이상거래탐지)" 항목에 직접 대응.

- 상태: 구현 + 재현 테스트(`FraudDetectionServiceTest` 8건) 완료
- 상세 내용: `version-update.md` "6. FDS(이상거래탐지) 미니 버전" 참고 (구조 결정 이유, 단계별 기술 선택 이유 포함)

## 2. 뉴스 분석 기능을 RAG + 평가 시스템으로 확장

현재(2026-07-31 구현)는 Claude API 단발 호출로 "오늘 뉴스 → 예상 등락 이유" 한 번 뽑아주는 수준(`news/NewsAnalysisService.java`). 이걸 아래처럼 확장하면 "LLM Agent/RAG 개발 및 평가 시스템 구축 경험"을 구체적 사례로 쓸 수 있음:

- 과거 뉴스 + 실제 다음날 주가 반응 사례를 저장해두고, 유사 사례를 검색(RAG)해서 근거로 같이 제공
- 모델이 예측한 방향과 실제 다음날 주가를 비교해서 정확도를 추적하는 간단한 eval 루프 추가

- 상태: 백로그 (아직 착수 안 함)

## 3. 설계 트레이드오프 문서화

캐싱 전략, 모델 선택(Claude Opus vs Haiku), 배치 주기(30초) 등 이번에 만든 설계들을 "왜 그렇게 결정했는지" 정리. JD가 강조하는 "아키텍처 트레이드오프 설명 능력"을 이력서/인터뷰에서 바로 쓸 수 있게.

- 상태: 백로그 (아직 착수 안 함)

---

## 7. 지정가 매수 자동 가격개선 통계 로깅 (2026-08-02)

### 배경/요구사항

매수 지정가의 5% 자동 가격개선 밴드(1-1 참고)가 실제로 지정가보다 얼마나 자주/얼마나 유리하게 체결되는지 물어봤는데, 이론적으로 고정된 확률이 없고(그때그때 호가창 상태에 따라 달라짐) 로컬 DB에도 아직 지정가 매수 체결 이력이 0건이라 실측치도 없는 상태였음. "지금 정확한 수치를 내는" 대신 "나중에 통계 낼 수 있게 기록부터 해두자"는 방향으로 정리.

### 설계 결정

- **DB 스키마 변경 없음** — 지정가(`Order.price`)와 실제 체결가(`Trade.matchedPrice`)가 이미 각각 테이블에 저장되고 있어서, 새 컬럼 없이 두 테이블을 JOIN하면 언제든 정확한 통계(개선 비율, 평균 개선율 등)를 다시 계산할 수 있음. 그래서 "기록"의 의미는 새 저장소를 만드는 게 아니라, 체결 시점에 바로 확인할 수 있는 로그 한 줄을 남기는 것으로 한정.
- FDS 때와 같은 이유로 새 메트릭 시스템을 붙이지 않고 기존 Loki 파이프라인에 그대로 흘려보냄 — 있는 관측 인프라를 재사용하는 이번 세션의 일관된 방향.

### 구현

- `MatchingService.execute()`의 체결 로그(`[체결]`) 직후, 지정가 매수(`isBuy && priceMode == LIMIT`) 체결 건에 한해 `logPriceImprovement()`를 호출.
- 개선율 = `(지정가 - 실제체결가) / 지정가 * 100` 을 계산해 `[가격개선] {주문ID} {종목코드} 지정가 {N}원 체결가 {M}원 개선율 {P}%` 형식으로 로그.
- 시장가 주문/매도 주문은 이 규칙 대상이 아니므로 스킵.

### 검증

- `./gradlew compileJava compileTestJava` 통과, 기존 `MatchingServiceTest` 4건 그대로 통과 확인.
- 로컬 DB(`~/stockTrade.mv.db`)를 H2 Shell로 직접 조회해서 확인: 현재 지정가 매수 체결 이력 0건, 전체 체결 2건(시장가 매수 위주) — 실측 데이터가 쌓이기 전이라는 것을 재확인. 실제 통계는 실사용 트래픽이 쌓인 뒤 `[가격개선]` 로그를 Loki/Grafana에서 집계하거나, `ORDERS`↔`TRADES` JOIN 쿼리로 언제든 정확히 계산 가능.

### 상태

- 로깅 구현 완료. 통계 집계(대시보드 패널화 등)는 사용자가 직접 하기로 함 — 필요해지면 후속 요청.

---

## 8. 버그 수정: 랭킹 캐시 무한 누적 (2026-08-02)

### 발견 경위

FDS/뉴스분석 기능 이후로 로컬 화면이 새로고침·개발자도구 토글·화면전환 시 10~20초씩 느려진다는 제보로 시작. 처음엔 FDS(신규 DB 쿼리 추가) 쪽을 의심했지만, 실제 조사 순서:
1. 서버 CPU/메모리/응답시간 확인 — 전부 정상, FDS 관련 에러/알림 로그도 0건.
2. `holding/stream`·`order/stream`에 연결/종료 로그를 추가해 좀비 SSE 연결 여부 확인 — 로그인 여부와 무관하게 느리다는 진술과 일치하게, 이 두 스트림은 애초에 열리지도 않았음(로그아웃 상태에서도 동일 증상) → SSE 좀비 연결 가설 기각.
3. Claude in Chrome으로 `localhost:8080/stockHome`을 직접 열어 DOM을 조사 — **DOM 노드 264,259개, `<tr>` 15,540개**를 발견. 정상이라면 랭킹 테이블은 상위 30개 안팎이어야 함.
4. `GET /api/volume/cached`로 서버 메모리의 랭킹 캐시 자체를 직접 확인 — `count: 15540`, `dataRank`가 1~30을 계속 반복 → 상위 30개 리스트가 약 518번 이어붙여진 상태임을 확인.

### 원인

- `KisService.restartFromDb()`가 앱 시작 시 "오늘 날짜 랭킹 데이터가 없으면" `StockRepository.findToByOrderByDate()`로 "가장 최근 데이터"를 가져오도록 되어 있었는데, 이 쿼리에 **날짜 필터가 없어서** 실제로는 `STOCK` 테이블에 쌓인 **모든 날짜**의 EOD 스냅샷을 통째로 반환하고 있었음. 메서드 이름과 주석("가장 최근 데이터 조회")은 "최근 하루치"를 의도했지만 구현이 그렇지 않았던 것.
- 게다가 `cachedData.addAll(dtos)`만 하고 `clear()`가 없어서, 이 잘못된 대량의 리스트가 그대로 랭킹 캐시에 누적됨.
- 이 프로젝트가 여러 날에 걸쳐 개발되면서 `STOCK` 테이블에 하루치씩 EOD 스냅샷이 계속 쌓였고(1-3 기능 이후 며칠 더 사용), 그 누적된 날짜 수만큼 캐시가 부풀어서 15,540개(≈30개 × 518일치)까지 커진 상태로 방치되어 있었던 것으로 보임 — 즉 **FDS/뉴스분석과는 무관하게 훨씬 전부터 존재했던 버그**이고, 이번에 데이터가 충분히 쌓이면서 처음으로 체감될 만큼 커진 것.
- 이 부풀려진 리스트가 SSE(`/api/volume/stream`)로 그대로 브라우저에 전달되어 `stockHome.html`의 `renderTable()`이 매번 15,540개 행을 렌더링 → DOM 노드 26만 개 → 새로고침/리사이즈(개발자도구 토글 포함)마다 브라우저가 이 거대한 DOM을 다시 레이아웃/페인트하느라 느려짐.

### 해결

- `StockRepository`: `findToByOrderByDate()`를 제거하고, 서브쿼리로 최신 날짜 하나만 정확히 골라내는 `findLatestDateSnapshot()`로 교체.
  ```java
  @Query("SELECT s FROM Stock s WHERE s.date = (SELECT MAX(s2.date) FROM Stock s2)")
  List<Stock> findLatestDateSnapshot();
  ```
- `KisService.restartFromDb()`: 위 메서드로 교체하고, `cachedData.addAll()` 전에 `cachedData.clear()` 방어적으로 추가(현재는 1회성 호출이지만, 재발 방지 차원).
- `STOCK` 테이블의 기존 이력 데이터 자체는 건드리지 않음(유효한 과거 기록이라 삭제할 이유 없음) — 문제는 "여러 날짜를 한꺼번에 읽어오는 쿼리"였을 뿐, 데이터 자체는 정상.

### 검증

- `./gradlew compileJava compileTestJava` 통과, `./gradlew test` 13건 중 12건 통과(나머지 1건은 IntelliJ에서 이미 떠있는 인스턴스의 H2 파일 락 충돌로, 이 변경과 무관한 기존 이슈).
- 코드 수정만으로는 **이미 떠 있는 서버의 메모리 캐시(15,540개)가 저절로 줄어들지 않음** — `restartFromDb()`는 앱 기동 시 1회만 실행되므로, 서버 재시작 후에야 정상화된 캐시로 다시 채워짐. 재시작 후 `GET /api/volume/cached`의 `count`가 30 안팎으로 돌아오는지, `stockHome`의 DOM 노드 수가 정상 범위로 줄었는지 확인 필요(사용자 재시작 후 재확인 예정).

### 교훈

- FDS를 의심하고 시작했지만, 실제로는 "관찰 → 가설 검증" 루프(6번 항목에서 정리한 바로 그 구조)를 그대로 따라가며 하나씩 가설을 기각해나간 끝에 전혀 다른 위치의 기존 버그를 찾아냄. 증상이 났을 때 가장 최근에 바꾼 코드부터 의심하는 건 합리적인 출발점이지만, 증거가 아니라고 말하면 미련 없이 다음 가설로 넘어가는 게 중요했음.

---

## 9. 뉴스 영향 분석(Claude API) 기능 - 실제 동작 검증 (2026-08-26)

### 발견 경위

포트폴리오/이력서에 "Claude API를 제품 기능에 직접 통합한 사례"로 `NewsAnalysisService`(2026-08-02, 6번 항목 참고)를 쓰려다가, "실제로 잘 작동하는지 검증한 근거가 있냐"는 지적을 받고 라이브로 확인해봄. 그 과정에서 이 기능이 **지금까지 한 번도 실제로 성공한 적이 없었다는 걸 발견함.**

### 원인 1 - API 키 미설정

- `application-secret.yaml:12`가 `apiKey: # TODO: ANTHROPIC_API_KEY 채워넣기` — 플레이스홀더만 있고 실제 키가 채워진 적 없음. 셸 환경변수 `ANTHROPIC_API_KEY`도 없음.
- 그래서 빈 문자열이 `x-api-key` 헤더로 전송돼 401이 났는데, `NewsAnalysisService.analyze()`의 `try/catch`가 이를 조용히 삼키고 `NewsAnalysisResponse.analyzing()`(화면엔 그냥 "분석 중")으로 처리해서, 사용자 입장에선 버그처럼 안 보이고 계속 숨어있었음.
- 최초 재현 로그:
  ```
  2026-08-25T13:39:06.242+09:00 ERROR ... NewsAnalysisService : [NewsAnalysis] Claude 분석 실패 code=005930 :
  401: {"type":"error","error":{"type":"authentication_error","message":"x-api-key header is required"},"request_id":"req_011CeQk3bF5mRAJS3STZBzuA"}
  ```

### 원인 2 - 크레딧 잔액 없음

- 실제 키를 발급받아 채워넣은 뒤 재기동하니 401은 사라졌지만, 이번엔 결제 잔액 부족으로 막힘:
  ```
  2026-08-26T13:45:14.635+09:00 ERROR ... NewsAnalysisService : [NewsAnalysis] Claude 분석 실패 code=005930 :
  400: {"type":"error","error":{"type":"invalid_request_error","message":"Your credit balance is too low to access the Anthropic API. Please go to Plans & Billing to upgrade or purchase credits."},"request_id":"req_011CeQkWjzgmA5kxtrkvq1L4"}
  ```
- Anthropic Console에서 결제 수단 등록 후 재확인 - 정상 동작 확인됨(아래 검증 참고).

### 검증 - 실제 Claude 응답으로 3가지 확인

**1) 정상 판단 사례 (삼성전자, 2026-08-26 당일 +3.02%)**

```
GET /api/news/005930/analysis?name=삼성전자
소요시간: 16.566초 (최초 호출, 실제 Claude API 왕복)

응답(JSON):
{
  "status": "COMPLETED",
  "direction": "UP",
  "reason": "삼성전자가 최대 110조원 규모의 주주환원책과 15조원 규모 자사주 매입 계획을 내놓으면서
    '기타법인' 매수세가 수급 버팀목으로 작용했고, 미래에셋증권 등 증권가에서 대규모 주주환원과
    커스텀 HBM 기술을 근거로 재평가 전망을 제시한 점이 투자심리를 자극한 것으로 보입니다.
    여기에 기관이 유가증권시장에서 1조9000억원대 순매수에 나서며 코스피가 2%대 급반등했고,
    삼성전자·SK하이닉스 등 대형 반도체주가 지수 상승을 주도하면서 삼성전자 주가도 장중 2~3%대
    강세를 보였습니다. 또한 HBM 슈퍼사이클에 따른 내년까지의 대규모 이익 전망, 우선주 소각
    가능성에 대한 기대 등도 매수세 유입 배경으로 언급됐습니다.",
  "articleTitles": [
    "코스피, 상승 전환해 6800선…삼전닉스 자사주 매입에 탄력",
    "\"삼성전자, 110조 주주환원·커스텀 HBM 앞세워 재평가 전망\"",
    "기관 순매수에 코스피 2%대 반등...삼성전자 2.72%, SK하이닉스 2.92% 상승",
    "코스피, 기관 1.9조원 순매수에 2%대 급등",
    "태우고 또 태운다…역대급 주주환원 SK하이닉스 다음 주자는",
    "HBM 슈퍼사이클의 역설…가격 치솟자 빅테크 '메모리 다이어트' [비즈...",
    "[시장 따라잡기] \"하반기 원전 뜬다\"…원전 잘 짓는 건설사는?"
  ]
}
```

**2) 캐싱 효과 (같은 종목, 같은 날 재호출)**

```
GET /api/news/005930/analysis?name=삼성전자 (2차 호출)
소요시간: 0.028초  (16.566초 → 0.028초, 약 591배)
서버 로그: NewsAnalysis 관련 로그 0건 - Claude API 재호출이 전혀 없었음(캐시 히트) 확인
```

**3) 무의미 판단 필터링 사례 (LG유플러스, 2026-08-26 당일 등락률 0.00%)**

```
GET /api/news/032640/analysis?name=엘지유플러스
소요시간: 5.649초 (Claude 실제 호출 후 응답 - 즉시 반환되는 "뉴스 0건" 경로가 아님)

응답(JSON):
{ "status": "ANALYZING", "direction": null, "reason": null, "articleTitles": [] }
```

- 뉴스 자체는 있었음(`GET /api/news/엘지유플러스` 직접 조회, 5건 반환) - 그 중 1건이 "토스 행운퀴즈 '엘지유플러스' 정답…8월 25일"로, **회사명만 우연히 언급됐을 뿐 주가와 무관한 기사**.
- 즉 "뉴스가 없어서 스킵"이 아니라, **뉴스는 있는데 Claude가 그게 등락(0.00%, 보합)을 설명할 근거가 안 된다고 정확히 판단해서 걸러낸 것** - 프롬프트에 명시한 "막연하거나 직접적 관련이 없어 보이면 유의미하지 않다고 판단하라"는 제약이 실제로 작동함을 확인.

### 교훈

- "동작은 하는데 검증된 적은 없는" 코드가 프로덕션에 조용히 방치될 수 있다는 걸 다시 확인함 - 예외를 우아하게 처리(`try/catch` → graceful fallback)하는 설계가 오히려 "한 번도 성공한 적 없음"을 몇 주간 가려버림. 에러를 삼키는 코드일수록, 정상 경로가 실제로 한 번은 성공하는 걸 직접 확인해두는 절차가 필요함.
- 포트폴리오에 "검증했다"고 쓰려면 실제로 그 순간 라이브로 돌려서 로그/응답을 남겨야 한다는 걸 이번에도 재확인 - 코드 리뷰나 설계 근거만으로는 "말로만 신뢰성 설계"에 머무름.
